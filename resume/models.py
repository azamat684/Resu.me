import uuid

from django.db import models


# class Resume(models.Model):
#     user = models.OneToOneField('users.User', models.CASCADE, 'resume')
#     uuid = models.UUIDField(default=uuid.uuid4())
